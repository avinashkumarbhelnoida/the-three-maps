# THE THREE MAPS — Phase III: Validation & Adversarial Testing

## Status
**COMPLETE — 201.81N–201.81P**

Phase III converts the architecture into an executable validation discipline.
It does not alter methodology, thresholds, semantic mappings, or the canonical
Fusion Strength formula merely to make tests pass.

## 201.81N — Golden Dataset Expansion & Calibration

- Golden Dataset `GDS-001` advanced to `GD-0.2.0`.
- Expanded regression catalogue to 45 deterministic scenarios.
- Added calibration utilities that compare observed engine output with expected
  values and report exact drift without mutating thresholds.
- Floating-point calibration tolerance is `1e-5`.
- Dataset validation continues to enforce unique IDs and required categories.

## 201.81O — Master Adversarial Test Suite

Executable invariants cover P0/P1 architecture rules, including:

- unavailable ≠ zero;
- invalid ≠ negative evidence;
- S6 has no Fusion penalty;
- only S7 can impose the contradiction penalty;
- 2-v-1 does not become convergence;
- shared evidence clusters do not become independence;
- semantic/context/temporal gates remain effective;
- weak opposition cannot create formal contradiction;
- aggregate polarity cannot create contradiction by itself;
- partial scores remain PARTIAL;
- duplicate active mappings are rejected;
- Numerology pattern candidates remain uniquely traceable;
- Palmistry model surface does not expose prohibited diagnostic/lifespan fields.

**Release rule:** P0 blockers must remain zero.

## 201.81P — End-to-End Three-Map Scenarios

Eight synthetic, non-personal scenarios exercise the common pipeline across
single-map, two-map, three-map, qualified, contradiction, unavailable, temporal
mismatch and semantic mismatch cases.

These scenarios are integration fixtures, not real-person readings and do not
assert that symbolic interpretations are scientifically true.

## Validation principle

The Phase III suite attempts to break the system at boundaries rather than only
confirming happy paths. A failure is treated as an architecture defect until
proven otherwise.
